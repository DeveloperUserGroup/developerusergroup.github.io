# Normal dev
- `yarn start` will get everything up and running. The server and all the transpilers.

# first runs
- `bundle install` - make sure you have jekyll & ruby installed
- `yarn install`

# Notes
If you update bootstrap, run `yarn run copy`